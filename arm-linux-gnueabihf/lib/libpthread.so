/* GNU ld script
   Use the shared library, but some functions are only in
   the static library, so try that secondarily.  */
OUTPUT_FORMAT(elf32-littlearm)
GROUP ( /opt/cross/arm-linux-gnueabihf/lib/libpthread.so.0 /opt/cross/arm-linux-gnueabihf/lib/libpthread_nonshared.a )
